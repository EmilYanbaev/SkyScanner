const initialState = {
    originInput:null,
    originHelp:[],
    destinationInput:null,
    destinationHelp:[]
}

let authReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
export default authReducer;